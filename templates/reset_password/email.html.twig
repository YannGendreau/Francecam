{% block content %}

	{% block title %}Francecam | lien de réinitialisation
	{% endblock %}


	<div class="container">

		<div class="logoSearch">
			<img src="{{asset('build/images/FRANCECAM-logo.png')}}" alt="logo du site Francecam">
			<h1>Francecam</h1>
		</div>
		<p>Pour changer votre mot de passe, visitez ce lien.</p>

		<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

		<p>Ce lien expirera dans
			{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
	</div>
{% endblock %}
