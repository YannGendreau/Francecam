{% extends 'navbar/layout.html.twig' %}

{% block title %}Francecam | Modifier
	{{film.title}}
{% endblock %}

{% block content %}
	<div class="container">
		<h1>Modifier</h1>
		<div class="formulaire">
			{{ include('film/_form.html.twig', {'button_label': 'Update'}) }}
		</div>
		<a href="{{ path('user') }}">retour à la liste</a>
		{# {{ include('film/_delete_form.html.twig') }} #}
	</div>

{% endblock %}
